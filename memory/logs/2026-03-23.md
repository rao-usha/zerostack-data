# Session Log — 2026-03-23

## 09:00 — Add acquisition story toggle to pe-demo.html (PLAN_031)

**What:**
- Added `.story-toggle` / `.toggle-btn` CSS to `<style>` block
- Added story toggle HTML (Exit / Disposition | Acquisition buttons) above the progress bar
- Wrapped all existing exit step divs (`step-0` through `step-3`) in `<div id="exit-story">`
- Added full `<div id="acq-story">` with 4 acquisition steps (A1 market scanner KPIs, A2 target discovery table, A3 company deep dive / thesis, A4 add to pipeline)
- Added JS: `switchStory()`, `acqNextStep()`, `acqPrevStep()`, `selectAcqTarget()`, `loadAcqStep1/2/3()`, `addAcqToPipeline()`

**Decisions:**
- Story toggle placed above `<nav class="progress-bar">` so it is always visible regardless of which exit step is active
- `exit-story` wraps only the step panels, NOT the progress bar or demo-nav, preserving existing navigation
- `API_BASE` aliased to existing `API` constant to avoid duplication

**Files changed:**
- `frontend/pe-demo.html`

**Next steps:**
- Task #5: Record 5-minute demo videos — acquisition story + disposition story
- Task #3: Verify dc-demo.html end-to-end in browser

**Blockers:** None

---
