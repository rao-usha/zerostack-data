# Session Log — 2026-03-27

## Agent B — PLAN_037 LP Conviction 2.0

**What:**
- Added `lp_tier` column (Integer, nullable) to `LpFund` model in `app/core/models.py`, placed after `lp_type` field. Values: 1=sovereign/endowment, 2=large_pension, 3=mid, 4=corp, 5=family_office.
- Expanded `ENDOWMENT_TARGETS` in `app/sources/lp_collection/form_990_pe_extractor.py` from 5 to 12 entries (added Duke, UMich, UVA, MacArthur, Ford, Rockefeller, Wellcome).
- Replaced `_extract_pe_investments_from_text` in same file with HTML-aware parser that delegates to `form_990_html_parser.parse_form_990_schedule_d` first, falling back to legacy regex.
- Updated `app/agents/fund_lp_tracker_agent.py`: changed default sources from `["pension_ir", "form_d", "form_990"]` to `["cafr", "form_990", "form_d"]`; added CAFR collection block; added `funds_scored` key to summary dict; added Step 4 scoring call after `_rebuild_relationships()`; added `_score_affected_funds()` method.

**Decisions:**
- Kept `pension_ir` block intact as fallback (not removed).
- `_score_affected_funds` capped at 20 GPs per run to avoid runaway costs.
- All DB operations in new method remain sync (`self.db.execute`) consistent with existing pattern.

**Files changed:**
- `app/core/models.py` (LpFund.lp_tier column)
- `app/sources/lp_collection/form_990_pe_extractor.py` (ENDOWMENT_TARGETS expanded, parser replaced)
- `app/agents/fund_lp_tracker_agent.py` (CAFR source, scoring integration, _score_affected_funds method)

**Next steps:** Agent A to handle any main.py integration if needed; run tests; restart API to pick up model changes.

**Blockers:** `form_990_html_parser.py` and `pension_cafr_collector.py` must exist for the new code paths to activate (graceful ImportError handling already in place).

---
