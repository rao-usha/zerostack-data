[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests (require API keys and network)
    unit: Unit tests (offline, no API keys required)
addopts = -v --strict-markers





