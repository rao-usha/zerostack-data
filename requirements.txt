# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
python-multipart==0.0.21

# HTTP client
httpx==0.25.2
aiohttp>=3.9.0,<4.0.0
tenacity>=8.2.0,<10.0.0

# Utilities
python-dateutil==2.8.2

# Scheduling
apscheduler==3.10.4

# Data processing (for IRS SOI Excel/CSV parsing)
pandas==2.1.3
openpyxl==3.1.2

# Kaggle API
kaggle==1.6.17

# Website contact extraction
beautifulsoup4==4.12.2
lxml>=5.1.0,<6.0.0
phonenumbers==8.13.27
robotexclusionrulesparser==1.7.1

# Agentic Portfolio Research (for LLM-powered extraction in Phase 3)
openai>=1.0.0,<3.0.0
anthropic>=0.5.0,<1.0.0
pdfplumber>=0.10.0,<1.0.0
pytesseract>=0.3.10,<1.0.0
pdf2image>=1.16.0,<2.0.0
PyPDF2>=3.0.0,<4.0.0
weasyprint>=61.0,<70.0
python-pptx>=0.6.23,<1.0.0
yfinance>=0.2.0,<1.0.0

# JS-rendered page support
playwright>=1.40.0,<2.0.0

# GraphQL API
strawberry-graphql[fastapi]>=0.220.0,<1.0.0

# Encryption (for API key storage)
cryptography>=42.0.0,<45.0.0

# Authentication
PyJWT>=2.8.0,<3.0.0
bcrypt>=4.1.0,<5.0.0
email-validator>=2.1.0,<3.0.0

# DNS verification (for email MX checks)
dnspython>=2.4.0,<3.0.0

# Geospatial (for Site Intelligence Platform)
geoalchemy2>=0.14.0,<1.0.0
shapely>=2.0.0,<3.0.0

# Statistical validation (for synthetic data quality checks)
scipy>=1.11.0,<2.0.0

# Parquet I/O for synthetic-data artifacts (PLAN_062 addendum 1)
pyarrow>=14.0.0,<20.0.0

# LightGBM distillation for synthetic consumer crowd (PLAN_062 addendum 1, Phase 2)
lightgbm>=4.0.0,<5.0.0

# scikit-learn — provides train_test_split, KFold, baseline regressors for distillation eval
scikit-learn>=1.3.0,<2.0.0

# ---------- PLAN_062 W3: Training deps for TabDDPM + Diffusion-TS ----------
# PyTorch with CUDA 12.x support. On linux/x86_64 PyPI ships CUDA wheels by default.
# Driver: Windows host 591.86, CUDA 13.1 visible; backward-compatible w/ CUDA 12.x torch.
torch>=2.4.0,<3.0.0

# Tensor shape manipulation — used by TabDDPM denoising net + Diffusion-TS transformer
einops>=0.7.0,<1.0.0

# Training experiment tracking (offline-friendly; can run without WANDB_API_KEY)
wandb>=0.16.0,<1.0.0

# ---------- Testing (not needed in production) ----------
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
